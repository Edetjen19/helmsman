# Helmsman control plane, Python 3.12 (runs in docker/python:3.12-slim).
# Host Python is broken; develop & test inside the container.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
jinja2>=3.1,<4.0
structlog>=24.1
python-multipart>=0.0.18

# dev / test (mock the Devin API, never call it in tests)
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
respx>=0.21,<1.0
